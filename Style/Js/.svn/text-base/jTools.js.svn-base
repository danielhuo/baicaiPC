formAlert = function(str, $obj, type) {
    $obj.children().remove();
    var $alert= $("<h4 class='text-" + type + "'>" + str + "</h4>");
    $alert.appendTo($obj).fadeOut(5000);
    return false;
};